<UserControl x:Class="WeatherApp.Views.FavouriteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WeatherApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <DropShadowEffect x:Key="CardShadow" BlurRadius="8" ShadowDepth="2" Opacity="0.3"/>
    </UserControl.Resources>
    
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>
            <TextBox
                Grid.Column="0"
                Margin="20"
                Width="300"
                Height="30"
                HorizontalAlignment="Left"
                FontSize="18"
                Text="{Binding LocationFilter, UpdateSourceTrigger=PropertyChanged}"/>

            <Button 
                Grid.Column="1"
                HorizontalAlignment="Right"
                Margin="0 0 40 0"
                Background="Transparent"
                BorderBrush="Transparent"
                BorderThickness="0"
                Cursor="Hand"
                FontSize="16"
                Width="40"
                Height="40"
                Command="{Binding NavigateHomeCommand}">
                <Image Source="/Assets/home.png" Stretch="Uniform" />
            </Button>
        </Grid>
        
        <ScrollViewer 
                Grid.Row="1"
                VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding FilteredLocations}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button
                            BorderThickness="0"
                            Cursor="Hand"
                            Focusable="False"
                            Command="{Binding DataContext.NavigateHomeWithLocationCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                            CommandParameter="{Binding}">

                            <Button.Background>
                                <ImageBrush ImageSource="{Binding IconUrl}" Stretch="Uniform" Opacity="0.2" />
                            </Button.Background>
                            
                            <Border Margin="10"
                                        Padding="10"
                                        CornerRadius="10"
                                        BorderBrush="#ccc"
                                        BorderThickness="1"
                                        Background="Transparent"
                                        Width="200"
                                        Height="150"
                                        Effect="{StaticResource CardShadow}">

                                <StackPanel VerticalAlignment="Top">
                                    <TextBlock Text="{Binding Location}" FontWeight="SemiBold" FontSize="14" />
                                    <TextBlock Text="{Binding Coord}" FontSize="12" />
                                </StackPanel>
                            </Border>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
